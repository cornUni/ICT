#!/bin/bash

# echo "Enter the target IP address or hostname: "
# read target

# target="127.0.0.1" # Set target to localhost

# echo "Enter the range of ports to scan (e.g., 1-1000): "
# read port_range

# # echo "Specify protocol (tcp/udp): "
# # read protocol

# echo "Scanning $protocol ports $port_range on $target..."
# nmap -sV -p $port_range -sU $protocol $target | grep -E "^(PORT|SERVICE)"
